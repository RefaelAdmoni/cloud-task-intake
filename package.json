{
  "private": true,
  "workspaces": [
    "api",
    "worker",
    "frontend"
  ],
  "dependencies": {
    "@openai/codex": "^0.137.0"
  },
  "devDependencies": {
    "eslint": "^8.0.0",
    "vitest": "^4.1.8"
  },
  "scripts": {
    "build": "npm run build --workspace api && npm run build --workspace worker && npm run build --workspace frontend",
    "build:api": "npm run build --workspace api",
    "build:worker": "npm run build --workspace worker",
    "build:frontend": "npm run build --workspace frontend",
    "test:api": "npm test --workspace api",
    "lint": "eslint './apps/**/*.{ts,tsx}'"
  }
}
